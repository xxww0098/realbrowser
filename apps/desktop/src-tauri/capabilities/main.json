{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal capability set for the local RealBrowser control window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
